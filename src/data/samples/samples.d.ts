/**
 * The sample library is a legacy JS module of `type_pack` → asset-URL string
 * exports (one per `import`ed .wav). Typed here as a flat string map; consumed
 * as a namespace import in `src/data/samples.ts`.
 */
declare const samples: Record<string, string>;
export = samples;
